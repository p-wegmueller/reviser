% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{fetch_alfred_data}
\alias{fetch_alfred_data}
\title{Download Real-Time Data from ALFRED and Format for ReviseR}
\usage{
fetch_alfred_data(
  series_id,
  series_name = NULL,
  observation_start = "1995-01-01",
  observation_end = "2024-12-01",
  realtime_start = "2015-01-01",
  realtime_end = "2022-01-01",
  api_key = NULL
)
}
\arguments{
\item{series_id}{A character vector specifying one or more ALFRED series IDs (e.g., c("CPIAUCSL", "PCEPI")).}

\item{series_name}{A character vector specifying custom names for the series (defaults to \code{series_id}).}

\item{observation_start}{A character string specifying the start date for observations (format: "YYYY-MM-DD").}

\item{observation_end}{A character string specifying the end date for observations (format: "YYYY-MM-DD").}

\item{realtime_start}{A character string specifying the start date for real-time vintages (format: "YYYY-MM-DD").}

\item{realtime_end}{A character string specifying the end date for real-time vintages (format: "YYYY-MM-DD").}

\item{api_key}{A character string containing a valid ALFRED API key (optional, defaults to \code{NULL}).}
}
\value{
A tibble with the following columns:
\itemize{
\item \code{ref_date}: The reference date (actual time of the observation).
\item \code{pub_date}: The publication date of the vintage (real-time data release date).
\item \code{id}: The series identifier (same as \code{series_name} if provided, otherwise \code{series_id}).
\item \code{value}: The reported value for the given observation and vintage.
}
}
\description{
This function retrieves real-time data from the Federal Reserve Bank of St. Louis ALFRED database
and formats it into a tidy structure suitable for analysis in the ReviseR package.
}
\examples{

# Fetch real-time CPI and PCE data from ALFRED
data <- fetch_alfred_data(
  series_id = c("CPIAUCSL", "PCEPI"),
  series_name = c("cpi", "pce"),
  observation_start = "1975-01-01",
  observation_end = "2024-01-31",
  realtime_start = "2000-01-01",
  realtime_end = "2024-01-31"
)

head(data)

}
