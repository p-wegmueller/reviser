% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reports.R
\name{generate_basic_report}
\alias{generate_basic_report}
\title{Generate a Basic Report on Data Revisions}
\usage{
generate_basic_report(
  data,
  output_file = "report.pdf",
  title = "Title of my basic report on data revisions",
  author = "Author(s)",
  include_logo = TRUE,
  logo_path = NULL,
  template_file = NULL
)
}
\arguments{
\item{data}{A data frame or tibble. The input dataset to be included in the report.}

\item{output_file}{A string. The name of the output file. Defaults to \code{"report.pdf"}.}

\item{title}{A string. The title of the report. Defaults to \code{"Title of my basic report on data revisions"}.}

\item{author}{A string. The name(s) of the author(s) of the report. Defaults to \code{"Author(s)"}.}

\item{include_logo}{A logical. If \code{TRUE}, a logo is included in the report. Defaults to \code{TRUE}.}

\item{logo_path}{A string. The file path to the logo image. If \code{NULL}, a default logo
provided by the \code{reviser} package is used. Ignored if \code{include_logo = FALSE}.}

\item{template_file}{A string. The file path to a custom LaTeX template for the report.
If \code{NULL}, the default template provided by the \code{reviser} package is used.}
}
\value{
A message indicating the location of the generated report.
}
\description{
This function creates a basic PDF report summarizing data revisions. The report
includes a title, author(s), and optionally a logo. It uses a customizable R Markdown
template and requires the \code{reviser} package's built-in templates.
}
\details{
\itemize{
\item \strong{Logo Handling}: If \code{include_logo} is \code{TRUE} and no \code{logo_path} is provided, a default
logo from the package templates is used.
\item \strong{Template Handling}: If a \code{template_file} is provided, it is used for generating the report;
otherwise, the default template is used.
}

The report is rendered using \code{rmarkdown::render()} with the \code{pdf_document} output format
and the \code{xelatex} LaTeX engine.
}
\examples{
\dontrun{
# Example usage with default options:
generate_basic_report(data = my_data)

# Example with custom options:
generate_basic_report(
  data = my_data,
  output_file = "custom_report.pdf",
  title = "Custom Report Title",
  author = "John Doe",
  include_logo = TRUE,
  logo_path = "path/to/logo.png",
  template_file = "path/to/template.tex"
)
}
}
